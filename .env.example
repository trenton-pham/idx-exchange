# Local development only. Production uses Secrets Manager and private networking.
DATABASE_URL=postgresql://idx:local-development-only@127.0.0.1:55432/housing
CORELOGIC_API_URL=https://replace-with-team-api-url
AUTH_ENDPOINT=https://replace-with-team-auth-endpoint
